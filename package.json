{
  "name": "ventuz-director",
  "version": "2.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && yarn build:main",
    "build:main": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "lint": "eslint . --ext .ts",
    "postinstall": "yarn build"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bitfocus/companion-module-ventuz-director.git"
  },
  "dependencies": {
    "@companion-module/base": "~1.4.0",
    "@types/websocket": "^1.0.5",
    "typescript": "~4.5",
    "websocket": "^1.0.34",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "@companion-module/tools": "^1.2.0",
    "@types/ws": "^8.5.4",
    "typescript": "~4.5"
  }
}
